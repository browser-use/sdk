---
title: API reference
description: "The full /api/v1 endpoint reference. Base URL https://api.browser-use.com/api/v1, auth via X-Browser-Use-API-Key."
icon: code
mode: wide
---

Every endpoint of the unified `/api/v1` API. Base URL:

```
https://api.browser-use.com/api/v1
```

Authenticate with your key on every request:

```
X-Browser-Use-API-Key: bu_your_key_here
```

The endpoint pages in this section are generated from the OpenAPI spec and include an interactive playground. For task-oriented guides, start with [Quickstart](/cloud/quickstart) and [Choose an agent](/cloud/choose-an-agent).

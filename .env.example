# Path to the internal cloud repo (contains backend + OpenAPI specs)
CLOUD_REPO_PATH=

# Local backend URL (for vibe testing + spec pulling)
BACKEND_URL=http://localhost:8000

# API key for the local backend (needed for vibe tests)
BROWSER_USE_API_KEY=

# Production API URL (for docs references only — never used in tests)
PRODUCTION_URL=https://api.browser-use.com
